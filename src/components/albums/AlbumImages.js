import React from 'react'

const AlbumImages = ({images}) => {
    return (
        <div>
           This will show a grid of all images in this album. 
        </div>
    )
}

export default AlbumImages
